#!/bin/bash

# Скрипт для создания репозитория на GitHub и отправки кода

cd ~/Biography

echo "🚀 Создание репозитория на GitHub..."

# Проверка авторизации
if ! gh auth status &>/dev/null; then
    echo "⚠️  Требуется авторизация в GitHub CLI"
    echo "Выполните: gh auth login"
    echo ""
    echo "Или создайте репозиторий вручную:"
    echo "1. Откройте: https://github.com/new"
    echo "2. Название: Biography"
    echo "3. Нажмите 'Create repository'"
    echo "4. Затем выполните: git push -u origin main"
    exit 1
fi

# Создание репозитория
echo "📦 Создаю репозиторий на GitHub..."
gh repo create Biography \
    --public \
    --description "Проект о том, как AI помогает создавать биографии для людей" \
    --source=. \
    --remote=origin \
    --push

if [ $? -eq 0 ]; then
    echo "✅ Репозиторий создан и код отправлен!"
    echo "🌐 Откройте: https://github.com/Shima337/Biography"
else
    echo "❌ Ошибка при создании репозитория"
    exit 1
fi
