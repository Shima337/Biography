# 🚀 Создание репозитория Biography - Решение

## Проблема

Для создания репозитория через API или GitHub CLI нужна авторизация. Есть 2 способа:

## ✅ Способ 1: GitHub CLI (рекомендуется, один раз)

Авторизуйтесь один раз, затем я смогу создавать репозитории автоматически:

```bash
cd ~/Biography
gh auth login
```

Выберите:
1. **GitHub.com**
2. **SSH** 
3. Авторизуйтесь через браузер (откроется автоматически)

После авторизации выполните:
```bash
./auto_create_repo.sh
```

## ✅ Способ 2: Personal Access Token (один раз)

1. Создайте токен: https://github.com/settings/tokens
2. Generate new token (classic)
3. Выберите scope: **repo**
4. Скопируйте токен

Затем выполните:
```bash
export GITHUB_TOKEN=ваш_токен_здесь
./create_repo_with_token.sh
```

## 🎯 После создания репозитория

После любого из способов репозиторий будет создан и код автоматически отправится на GitHub.

**URL**: https://github.com/Shima337/Biography

## 💡 Почему нельзя без авторизации?

GitHub API требует аутентификации для создания репозиториев (безопасность). SSH-ключ работает для push/pull, но не для создания репозиториев через API.
